name: Release

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:

  release:

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: install deps
        run: |
          pip install "python-semantic-release>=9.0,<10" build

      - name: debug git
        run: |
          git log --oneline -5
          git tag

      - name: semantic release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PYPI_TOKEN: ${{ secrets.PYPI_API_TOKEN }}
        run: |
          semantic-release version --push
          echo "PYPI_TOKEN is set: ${{ secrets.PYPI_API_TOKEN != '' }}"
          pip install twine
          twine upload dist/* --non-interactive    # ← test PyPI upload direct
          semantic-release -vv publish
